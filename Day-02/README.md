# Day 2 – Variables & Data Types

## 📚 Topics Covered

- Variables in Python
- Naming rules for variables
- Built-in data types
  - Integer (`int`)
  - Float (`float`)
  - String (`str`)
  - Boolean (`bool`)
- Using the `type()` function
- Type Casting
  - `int()`
  - `float()`
  - `str()`
- Taking user input using `input()`
- Printing output with `print()`

## 💻 Practice Programs

- Declared and initialized variables
- Stored different types of data
- Checked variable data types
- Converted data from one type to another
- Accepted user input and displayed the output

## 🎯 Key Learnings

- Understood how Python stores different types of data.
- Learned the importance of choosing meaningful variable names.
- Explored type conversion and user input handling.
- Built a strong foundation for writing Python programs.

## 🚀 Next Step

Continue learning conditional statements (`if`, `elif`, `else`) and apply them in simple decision-making programs.

---

**Day 2 of my Python AI Journey. Every day, one step closer to becoming an AI Engineer. 🚀**
